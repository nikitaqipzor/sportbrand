# Sprint 1 test plan

1. API config rejects cleartext production URLs.
2. Set validation rejects invalid weight, reps and RIR.
3. Outbox returns only current-user items and logout purges that user's items.
4. Contract contains idempotent set-write endpoint.
5. Android smoke: Today opens active workout and the primary action is reachable.
